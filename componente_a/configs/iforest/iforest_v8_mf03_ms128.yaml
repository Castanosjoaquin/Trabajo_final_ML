model: iforest
name: iforest_v8_mf03_ms128
cultivo: ambos

# Tuning: mejor max_features (0.3) con max_samples=128 (punto intermedio entre
# 64 y el auto=256).
n_estimators: 200
max_samples: 128
max_features: 0.3
contamination: auto
random_state: 42

n_seeds: 10
threshold_mode: contamination
eval_contamination: 0.10
